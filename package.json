{
  "name": "aig",
  "private": true,
  "scripts": {
    "dev:docs": "pnpm --filter @aig/docs dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
